# --- SEC EDGAR (required, free — just identify yourself per SEC policy) ---
EDGAR_USER_AGENT="FinSight research project subham.tiwari186@gmail.com"

# --- Azure OpenAI (Phase 1: embeddings + answers) ---
AZURE_OPENAI_ENDPOINT="https://<your-resource>.openai.azure.com/"
AZURE_OPENAI_API_KEY="<key from Azure portal>"
AZURE_OPENAI_API_VERSION="2024-10-21"
AZURE_OPENAI_CHAT_DEPLOYMENT="gpt-4o"
AZURE_OPENAI_MINI_DEPLOYMENT="gpt-4o-mini"
AZURE_OPENAI_EMBED_DEPLOYMENT="text-embedding-3-large"

# --- Postgres + pgvector (docker compose up -d in infra/) ---
DATABASE_URL="postgresql://finsight:finsight@localhost:5432/finsight"
